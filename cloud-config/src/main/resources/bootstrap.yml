server:
  port: 8081
spring:
  application:
    name: config-server
  profiles:
    active: native
  cloud:
    config:
      server:
        native:
          searchLocations: classpath:/conf/
eureka:
  client:
    serviceUrl:
      defaultZone: http://user:123456@localhost:8080/eureka/      #???????????????????BASIC????????????????
#instance:
#  prefer-ip-address: true         #??IP??????????????